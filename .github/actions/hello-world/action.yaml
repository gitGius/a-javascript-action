name: "my javascript action"

description: "simple greeting with github"

inputs:
  first-greeting:
    description: "who would you like to greet with console"
    required: true
    default: "Hubot"
  second-greeting:
    description: "another person to greet"
    required: true
    default: "Mona"
  third-greeting:
    description: "a third greeting"
    required: true
    default: "Testesdapri"
  last-one-greeted:
    description: "the person greeted last"
outputs:
  last-one-greeted:
    description: "the person greeted last"
runs:
  using: "node12"
  main: "index.js"
